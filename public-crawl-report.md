# 参考页面公开设置采集报告

## 采集范围与边界

本报告由对用户提供页面公开返回的 HTML、CSS、运行时 DOM、计算样式、媒体查询和资源 URL 路径进行系统采集后生成。采集脚本位于 `/home/ubuntu/reference-study/crawl-public-settings.mjs` 与 `/home/ubuntu/reference-study/crawl-public-states.mjs`。原始信纸图像、蜡封图像、文案、字体文件、JavaScript 源码和品牌素材均未下载、嵌入或复用。[1]

> 这份数据可以精确指导布局与交互重建，但**不能授权复制原站受保护的视觉资产或内容**。后续实现只能使用原创纸张、原创文字和独立代码。

## 公开结构清单

| 项目 | 实测结果 | 含义 |
| --- | ---: | --- |
| 桌面采样视口 | 1440 × 857 px | 对应当前桌面布局基准。 |
| 桌面轨道 | 1152 × 23,191.5 px | 宽度为视口的 80%，不是当前项目的 10,060 px 短轨道。 |
| 移动采样视口 | 375 × 812 px | 使用设备模拟获得的真实 CSS 视口。 |
| 移动轨道 | 375 × 24,526 px | 改为相对定位的单列流，内边距为 `1px 18px 120px`。 |
| 信件数量 | 50 封 | 当前项目的 12 封不足以复现完整纵向节奏。 |
| 命名纸张变体 | 8 类以上 | 已确认包括 rag-paper、plain-ivory、service-blue、close-written-fold、office-typewriter、correspondence-card、ruled-notebook、red-folded。 |
| 公开 CSS 文件 | 3 份 | 包含桌面、移动、阅读器、蜡封与动画规则。 |
| 公开脚本文件 | 12 个 | 仅登记路径，不复制或运行其源码。 |
| 公开信纸资源路径 | 至少 60 条 | 仅作为清单记录，不下载或用于本站。 |

## 桌面布局的精确参数

参考站使用固定的独立滚动容器：`vertical-viewport` 为全屏 fixed 元素，垂直滚动而隐藏滚动条；`vertical-track` 是相对定位的超长画布。信件通过每项独立 CSS 变量完成定位和视差，而非规则网格。前六封信的真实参数如下。

| 序号 | 宽度 | Y 坐标 | X 坐标 | 水平偏移 | 视差速率 | 纸张变体 |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 224 px | 96.82 px | 17.92% | -53.36 px | -0.0200 | rag-paper |
| 2 | 356 px | 693.49 px | 15.15% | +61.40 px | +0.0033 | plain-ivory |
| 3 | 510 px | 270.94 px | 78.92% | -17.33 px | -0.0167 | service-blue |
| 4 | 268 px | 1318.05 px | 19.41% | +76.98 px | +0.0067 | close-written-fold |
| 5 | 438 px | 1451.95 px | 77.78% | -55.09 px | -0.0133 | rag-paper |
| 6 | 196 px | 2170.03 px | 51.28% | +30.99 px | +0.0100 | plain-ivory |

移动端将相同数据改为相对定位：首封信实际坐标为 `x=47.5px`、`y=136.65px`、宽 224 px；第二封信为 `x=68px`、`y=574.45px`、宽 295 px。超宽信纸被限制为约 `100vw - 80px`，并以约 ±28 px 横向错位形成连续流。

## 交互状态的精确参数

| 状态 | 参考页面公开状态 | 当前项目状态 | 必要修正 |
| --- | --- | --- | --- |
| 蜡封默认 | 200 px 元素以 `scale(.5)` 显示，实际约 100 × 100 px；固定 `top:18px`、`left:-13px`。 | 已接近。 | 将背景色与开启定位对齐。 |
| 蜡封开启 | 类名 `archive-seal is-open`，实显 400 × 400 px，位置 `x:520px`、`y:228.5px`，背景层不透明度 1、色值 `rgba(247,247,245,.72)`、时长 0.5 s。 | 当前尺寸与交互逻辑接近，但不是精确位置与状态锁定。 | 以实测变量替换近似中心定位。 |
| 档案滚动 | 每封信在滚动之外还应用独立 Y 轴视差；采样 1100 px 时，卡片 1 的额外位移为 -25.64 px。 | 目前只覆盖 12 封近似参数。 | 采用完整 50 封参数表与 23,191.5 px 轨道。 |
| 阅读器打开 | 滚动容器加入 `has-selection is-intro-locked`，但保持打开前的 `scrollTop`。 | 已实现位置保留。 | 用与参考相同的锁定类语义和阅读器尺寸。 |
| 阅读器桌面 | 舞台 1050 px，网格 `640px 320px`，间隙 56 px，内边距 `56px 0 48px`；右侧信息栏 sticky。 | 当前使用近似比例而非精确网格。 | 固定为实测网格，右栏改 sticky。 |
| 阅读器关闭 | 点击固定全屏 `reader-dismiss-layer`，鼠标为 zoom-out；阅读器移除，滚动位置保持 1100 px。 | 已实现关闭与位置保留。 | 对齐过渡、层级和桌面长页舞台。 |

## 当前实现与完整重建之间的差异

当前版本不能称为完整复刻。最大的结构差异不是一个小样式问题，而是数据规模与渲染策略：参考站有 50 封不同尺寸、不同纸张变体、不同多页层次的信件，轨道高度约为当前的 2.3 倍；当前版本只有 12 封 CSS 生成的信纸。参考阅读器会保留原信纸的多页堆叠，而当前阅读器只呈现重新排版的文字纸张。上述差异必须逐项重建，才可在不复用原图资产的前提下获得接近的结构与互动节奏。

## 可执行的下一轮重建规格

下一轮应建立包含 50 项原创信件元数据的配置文件，逐项采用已采集的 `width`、`x`、`x-wide`、`y`、`offset`、`gap`、`parallax-rate` 和 `variant` 参数。桌面端轨道应调整为 23,191.5 px，移动端改为 24,526 px 的相对流，并使用参考站的 18 px 内边距、80 px 最大宽度约束和 ±28 px 交错偏移。阅读器应改为 640/320/56 的实测网格和 sticky 信息栏，同时保留关闭层、锁定状态与滚动位置恢复。

## 参考

[1] [Letters of Love — 公开页面](https://joshpuckett.me/letters-of-love)
