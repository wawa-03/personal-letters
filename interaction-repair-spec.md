# 快速交互与纸张叠放修复规格

本规格针对用户在实测中发现的三项问题：快速滚动时的相对间距、连续开关的动画竞争、以及斜向印迹和过度分散的纸张堆叠。

| 问题 | 修复机制 | 验收标准 |
| --- | --- | --- |
| 高速滚动时视差放大相邻间距变化 | 使用一个全局、时间连续的 `smoothedScroll` 驱动所有信件；每封信采用 `base + smoothedScroll × clampedRate`，并将额外位移钳制在其公开 `gap` 的 18% 内。 | 连续大幅滚动时，卡片相对位移平滑收敛，且任一信的视差不越过本身局部间距保护范围。 |
| 连续打开和关闭出现重叠 | 使用 `idle → opening → open → closing` 单一状态机；关闭开始后输入门控，过渡完成才卸载；如果有新的打开请求，只保留最后一个待处理意图。 | 快速连续触发时，页面上只存在一套阅读器；旧计时器不能覆盖后来的状态。 |
| 堆叠像斜向扇开 | 档案态最多展示 3 页，逐页在中心附近仅作小幅 X/Y 偏移；旋转压缩到 ±0.35°。 | 2–3 页从中心向下自然叠起，不形成明显斜向扇形。 |
| 纸张印迹方向不自然 | 光线、压痕和折线只使用 0°、90° 的水平/垂直渐变；去除 35°、105°、145° 的大幅斜向渐变。 | 纸面保留细微横向/纵向印迹和折痕，不出现贯穿整张的斜线。 |

## 运动参数

平滑时间常数采用 42 ms；每帧系数使用 `1 - exp(-Δt / 42)`，使快速滚动时的额外视差有短促且可预测的追随感，但不影响滚动容器本身的原生滚动。公开速率被限制到 `[-0.014, 0.014]`，以避免不同方向的极端卡片在高速滚动下把局部间距拉开。

## 交互门控

阅读器从打开到完全关闭期间，档案层保持不可点击；关闭中的阅读器也不接收再次关闭操作。所有计时器附带递增令牌，只有最新令牌可提交状态，防止旧的 560 ms 关闭回调在新的打开请求之后错误卸载阅读器。
