# 交互复刻状态机

本规格仅包含已由参考站运行状态确认的行为。实现采用原创信纸与个人内容，但每一个状态转换、滚动锁定和关闭恢复都必须按本表验证。

| 状态 | 触发 | 参考站的实际表现 | 本次实现验收标准 |
| --- | --- | --- | --- |
| `canvas` | 页面初始或阅读器关闭后 | 全屏固定画布，独立 `vertical-viewport` 可滚动，浏览器滚动条隐藏，左上蜡封常驻。 | 仅 `.archive-viewport` 负责纵向滚动；信件轨道不可被普通页面滚动替代。 |
| `canvas-scrolling` | 用户滚动 | 每封信同时经历轨道滚动和其独立的低幅度 `translateY` 视差；参考采样值在滚动 1100 px 时介于约 -25.6 至 +2.8 px。 | 以每封信的固定视差速率乘当前 `scrollTop`；效果必须在静止时归零且不造成布局抖动。 |
| `reader-opening` | 点击信件 | 原档案容器增加 `has-selection` 和 `is-intro-locked`；原滚动位置保留；信纸向桌面阅读舞台过渡。 | 保存当前 `scrollTop`，禁用档案输入并进入阅读层；过渡使用 280–500 ms 的透明度与 transform。 |
| `reader-open` | 打开过渡完成 | 阅读层覆盖全屏。桌面内容舞台约 1050 px 宽，左侧约 640 px 是放大信纸，右侧约 320 px 为标题、简介、分隔线与元数据。 | 阅读器维持左纸右信息的 2:1 结构；移动端折叠为单栏并提供显性关闭按钮。 |
| `reader-close` | 点击全屏 `reader-dismiss-layer`、移动关闭按钮或 Esc | 阅读层退场；阅读器 DOM 移除；档案容器恢复为 `vertical-viewport`，并保持打开前的精确 `scrollTop`。 | 关闭后恢复先前滚动位置、焦点和独立视差；不跳回顶部，也不留下滚动锁定类名。 |
| `seal-open` | 点击固定蜡封 | 档案整体降亮度/轻失焦，中央出现放大约 240 × 374 px 的金色椭圆简介。 | 该交互与阅读器互斥；点击背景或关闭控制可返回 `canvas`。 |

## 阅读器空间规格

桌面端阅读器采用 `min(1050px, 100vw - 80px)` 舞台宽度和两列比例。信纸面板使用约 64% 的宽度，信息栏使用约 30%，其间保留约 6% 的空白。信纸应该在打开后保持与档案卡片相同的纸张变体和多页层次，而不是被替换成通用弹窗。关闭层必须覆盖整个视窗，并在鼠标指针位于信纸和信息栏以外时可点击。

## 视差规格

每封卡片设置一个小幅速度值，范围为 `-0.0233` 至 `0.0033`。在桌面端，视差应用在卡片的 Y 轴平移，组合其原有水平漂移与微小旋转；移动端禁用该 Y 轴附加位移。当前轨道最大高度保持 10,060 px，可随原创内容数量增长，但不得将所有卡片重排为规则列表。
