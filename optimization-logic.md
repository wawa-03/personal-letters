# 运动与材质校准逻辑

本轮校准以用户提供页面的公开 DOM、CSS 和运行时计算样式为依据，不下载或复用其书信图像、纹理文件、字体文件或 JavaScript 源码。[1]

## 滚动与视差

| 对标可验证行为 | 本地校准策略 | 目的 |
| --- | --- | --- |
| 每封信有独立 `data-parallax-rate`。 | 将 50 封信的公开速率写入配置。 | 保留不同纸张相对滚动的快慢关系。 |
| 在多个 `scrollTop` 采样点中，额外 Y 位移满足「初始基线 + 速率 × 滚动位置」的线性关系。 | 每封信新增 `parallaxBase`，按 `base + scrollTop × rate` 更新。 | 修正原先所有卡片从 0 位移开始的差异。 |
| 卡片本身不使用 CSS 的 transform transition；滚动容器使用浏览器原生滚动。 | 通过 `requestAnimationFrame` 直接写 CSS 自定义属性，不在滚动中触发 React 全树重渲染，也不额外添加低通滤波。 | 避免人为“阻尼”导致纸张拖尾，保持原生滚动的即时性。 |
| 卡片初次进入为 `680ms cubic-bezier(.22,1,.36,1)`，由 `translateY(56px) scale(.985)` 进入。 | 使用相同时长、缓动和起始位移。 | 让初次出现的节奏接近公开样式。 |

> 这里的“阻尼感”主要来自**浏览器自身的触控/滚轮惯性与不同速率纸张的相对位移**，而不是对每一张纸做独立的 CSS 滞后动画。人为加入平滑滤波会使它比对标站更黏滞。

## 信件打开与关闭

| 对标可验证行为 | 本地校准策略 |
| --- | --- |
| 档案层在选中后于约 240ms 淡出并禁止滚动。 | 选中时对滚动视窗应用 240ms opacity 退场及输入锁定。 |
| 阅读器表面使用 `560ms cubic-bezier(.22,1,.36,1)` 进出。 | 阅读器保持在 DOM 中 560ms，关闭时添加 `is-closing` 再移除，避免立即消失。 |
| 右侧元数据为 360ms 进入，延迟 220ms；关闭为 220ms 淡出。 | 信息栏使用相同的延迟、时长和可逆出场。 |
| 关闭后阅读器被移除，原滚动容器保持原来的 `scrollTop`。 | 不重置滚动容器；端到端验证中关闭后仍保留 `scrollTop = 1100`。 |

## 纸张质感

纸张效果拆为四层：原创纸张底图提供纤维、边缘和折痕；低透明度渐变提供光线扫过的纸面变化；径向污渍与细线提供局部年代感；边框和三层柔和阴影提供纸张厚度。不同变体分别校准了公开 CSS 中可见的纸色、墨色、行距、折痕和格线规则，例如 rag-paper 的蓝墨与棉纸色、service-blue/close-written-fold 的奶油纸色与折痕、ruled-notebook 的红边与蓝线。

档案态的每一页依据公开采集的 `x / y / rotation / z-index` 单独绝对叠放；阅读态将同一组页按 28px 间隔在 640px 画布上展开。这样既保持档案中纸张厚度，也保留展开阅读时的逐页节奏。

## 已知边界

对标站另有专用图形层把其原始书信图片过渡到阅读器。该层及原始扫描图像属于对方资产，本项目不复制。因此，本版本复现的是公开可验证的布局、速率、时序、材质分层和状态机，并以原创纸张资产替代专有画面，不应被表述为像素级或 100% 资产级复制。

## 参考

[1] [Letters of Love — 用户提供的公开页面](https://joshpuckett.me/letters-of-love)
