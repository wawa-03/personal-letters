# 材质与移动端校准规格

## 纸张压痕与边缘

水平折痕由四层同轴渐变构成：折线中心使用低不透明度的暖灰凹陷阴影；中心上方使用窄幅高光；折线两侧在 10–18 px 范围内以极低不透明度回落；最底层保留原创纸纹。垂直折痕采用相同机制，但仅用于折叠纸变体。任何折痕都不能使用倾斜角度。

边缘做旧不应只是规则边框。本次采用独立的边缘遮罩层：四边叠加低不透明度的内阴影、零散边缘污渍和不均匀色差；纸角再以局部径向污渍轻微加深。所有效果必须保持在内容层之下，并且不会压暗书写文字。

| 项目 | 参数方向 | 验收标准 |
| --- | --- | --- |
| 水平折痕中心 | 暖灰阴影，不透明度不高于 0.18 | 像压下去的细线，而不是色带。 |
| 折痕高光 | 纸白，不透明度不高于 0.16 | 与凹陷阴影形成轻微起伏。 |
| 过渡宽度 | 中心两侧约 12–18 px | 无硬边、无斜向贯穿。 |
| 边缘污渍 | 四边渐变及四角径向渐变 | 在缩略图中可感知，在文字区不抢眼。 |
| 纸张叠放 | 2–3 层居中，Y 偏移约 2–5 px | 形成厚度，而非散开的扇形。 |

## 移动端滑动与阅读器

375 × 812 视口的实测基线显示，当前阅读器能够保持 `scrollTop = 4600` 的原位恢复，页宽约为 319 px。移动端使用比桌面更短的 34 ms 视差跟随常数、较窄的速率范围 `[-0.0105, 0.0105]`，并把额外偏移限制到相邻间距的 14%（18–42 px），避免窄屏时视觉距离被放大。

阅读器保留 560 ms 的状态机周期以避免竞态，但移动纸面使用 480 ms、12 px 起始位移和 0.985 缩放；右侧信息在移动端改为 300 ms、160 ms 延迟的单列渐入。这样视觉上更紧凑，但逻辑门控仍等待完整关闭周期，保证连续点击不会留下重叠层。
