# 参考页面核验记录

## 已确认的首屏与信息架构

参考页面并非传统的编辑式内容列表或落地页。其主体是一个占满整个视窗宽度的**纵向、无边框的漂浮信件档案**：暖白色背景上排列真实信笺形态的卡片，卡片大小、纸张颜色、旋转角度、水平位置和垂直间距均不规则。页面在首屏不展示标题、导航、说明区或页脚。

左上角有一枚独立、固定的竖向椭圆蜡封；它是唯一常驻界面元素。页面源码显示，蜡封正反两面可翻转，背面包含一段介绍文本。点击每封信进入阅读模式。正文流总高度约 22,894 px，以纵向滚动持续浏览。

## 已确认的首屏构图（1440 × 1000）

页面背景为接近白色的暖灰。左上角蜡封尺寸约 60 px × 96 px。首屏中的信件至少三封：左上是窄幅浅米色手写信，中右是大幅金黄色手写信，下左有一封较大的白色信。它们不对齐网格，保留大量空白。信件本身以高对比度深色手写体呈现，纸张有真实边缘和多页轻微错位。

## 移动端与蜡封开启状态

在 375 px 宽的移动端，固定蜡封仍位于左上，尺寸约为 60 px × 96 px。信件从顶部开始改为单列纵向流：首封浅米色信件宽约 224 px，第二封白色信件宽约 300 px，左右以温和的错位取代桌面端的大跨度散点。背景和卡片质感保持不变。

蜡封被点击后，页面的信件背景会整体淡化，中央出现约 240 px × 374 px 的金色竖向椭圆面。该椭圆承载简短说明文字，内容为深褐色衬线体；背景没有明显的黑色蒙层，仅保留极轻的失焦和降亮度。再次点击背景或蜡封可收起说明并恢复档案流。

## 公开源码中确认的交互与实现线索

页面使用 `vertical-archive`、`vertical-viewport`、`vertical-track` 和 `vertical-letter-slot` 组成独立滚动轨道。信件按 `--letter-width`、`--letter-x`、`--letter-y`、`--letter-gap` 和 `--letter-offset` 自定义定位，并带不同 `data-parallax-rate`。每一封信都是可点击按钮，内部是可展开的多页信笺堆叠。信笺样式包含 rag-paper、plain-ivory、service-blue、close-written-fold、office-typewriter、correspondence-card、ruled-notebook 与 red-folded 等变体。

## 复刻边界

后续实现将严格复刻已确认的布局逻辑、空间节奏、滚动与展开模式，但不会引用、下载或发布参考站的蜡封图、书信图、原始文案、字体文件或源代码。用户的原创表达将被渲染为自有纸张卡片，并使用新生成的原创视觉资产。
