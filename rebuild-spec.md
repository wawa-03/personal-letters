# 高保真复刻规格

本规格以已核验的参考页面截图、公开渲染结构和公开样式行为为准。目标不是保留原先的“个人表达编辑站”框架，而是改为一个无导航、无标题首屏、由信件本身构成内容入口的纵向档案。

| 范畴 | 参考页面的已核验特征 | 本次实现标准 |
| --- | --- | --- |
| 画布 | 固定全屏暖灰白画布，内容在独立纵向滚动容器内。 | `#f6f6f4` 画布，全屏无导航的滚动档案。 |
| 固定入口 | 左上角一枚窄椭圆蜡封，约 60 × 96 px；默认位于左侧略微出血。 | 自有烫金质感椭圆章，尺寸、位置、悬停放大和固定逻辑相同，但不使用原图案或名称。 |
| 信件构图 | 桌面端信件散落在约 22,000 px 的纵向轨道上；首屏有 224 px 窄信、510 px 宽信与下方白色信。 | 建立约 10,000 px 轨道；前 8 封信使用同样的大小梯度、左右节奏和大面积留白。 |
| 信纸 | 纸张有米白、奶油黄、服务蓝、红纸、格线纸和明信片等多种变体；多页信件呈现约 1–2 px 的错位堆叠。 | 用 HTML 与 CSS 生成原创纸张、阴影、边缘和错页；不使用参考站书信图片。 |
| 文字 | 信面以细小手写英文为主，辅以少量打字机式文本。 | 用原创中文短笺、手写中文字体与少量英文字母日期，保证阅读内容归属用户。 |
| 桌面布局 | 以绝对定位散点排列；大卡片可达约 510 px，横向中心点大致在 18%、51%、79% 和 90% 之间。 | 使用每张信独立的 `x / y / width / drift / rotation` 配置，复刻非网格的节奏。 |
| 移动布局 | 变为居中的单列流，左右以最多约 28 px 偏移；信件最大宽度为视窗减去 80 px。 | 使用断点切换为文档流，保留首封窄信和第二封大信的比例、间距与错位。 |
| 蜡封弹层 | 点击后信件背景变淡，中央出现放大的金色竖向椭圆，展示短介绍；点击外部关闭。 | 用 CSS 3D 翻面与轻微背景失焦，展示用户个人简介，避免原文。 |
| 阅读互动 | 信件是可点击入口；聚焦后成为阅读主体。 | 点击信纸会在居中阅读层展示该条原创完整表达；Esc 和背景点击关闭。 |
| 动效 | 信件随滚动有轻微差异化视差；蜡封悬停放大，翻转约 500ms。 | 仅做低幅度、性能安全的 CSS 视差与 3D 翻面；尊重减少动态偏好。 |

## 实施验收点

桌面端首屏必须一眼呈现为“左上蜡封 + 无规则飘浮手写信件 + 大面积暖白留白”，而不是内容卡片列表。移动端首屏必须转为单列信纸流，固定蜡封保持左上。点击蜡封必须出现中央金色椭圆简介；点击任一信件必须以阅读层打开原创文本。现有顶栏、宣传 Hero、深色引言、分类筛选和页脚将全部移除。
